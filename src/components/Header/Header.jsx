import React from "react";
import styles from './Header.module.css';

const Header = () => {
    return <header className={styles.header}>
      Система учета запросов
    </header>
}

export default Header;